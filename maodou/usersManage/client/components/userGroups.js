import React, { Component, PropTypes } from 'react';

export default class UserGroups extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return(
      <div>
        <h1>小组</h1>
      </div>
    );
  }
}
